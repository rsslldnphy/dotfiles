[core]
  editor = vim
  whitespace = trailing-space,space-before-tab
  excludesfile = ~/.cvsignore

[merge]
  mergeoptions = --no-ff

[user]
  name = Russell
  email = me@rsslldnphy.com

[alias]
  co = checkout
  c  = commit -v
  ci = commit -m
  aa = add -A
  psuh = push
  pretty = log --oneline --graph --decorate --all

[push]
  default = upstream

[credential]

[hub]
  protocol = git

[filter "lfs"]
  required = true
  clean = git-lfs clean %f
  smudge = git-lfs smudge %f
